// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License.

// <auto-generated/>

#nullable disable

using System.Collections.Generic;
using System.Text.Json;
using Azure.Core;

namespace Microsoft.Azure.IoTCentral.Preview.Models
{
    public partial class User : IUtf8JsonSerializable
    {
        void IUtf8JsonSerializable.Write(Utf8JsonWriter writer)
        {
            writer.WriteStartObject();
            writer.WritePropertyName("type");
            writer.WriteStringValue(Type);
            writer.WritePropertyName("roles");
            writer.WriteStartArray();
            foreach (var item in Roles)
            {
                writer.WriteObjectValue(item);
            }
            writer.WriteEndArray();
            writer.WriteEndObject();
        }

        internal static User DeserializeUser(JsonElement element)
        {
            if (element.TryGetProperty("type", out JsonElement discriminator))
            {
                switch (discriminator.GetString())
                {
                    case "adGroup": return ADGroupUser.DeserializeADGroupUser(element);
                    case "email": return EmailUser.DeserializeEmailUser(element);
                    case "servicePrincipal": return ServicePrincipalUser.DeserializeServicePrincipalUser(element);
                }
            }
            Optional<string> id = default;
            string type = default;
            IList<RoleAssignment> roles = default;
            foreach (var property in element.EnumerateObject())
            {
                if (property.NameEquals("id"))
                {
                    id = property.Value.GetString();
                    continue;
                }
                if (property.NameEquals("type"))
                {
                    type = property.Value.GetString();
                    continue;
                }
                if (property.NameEquals("roles"))
                {
                    List<RoleAssignment> array = new List<RoleAssignment>();
                    foreach (var item in property.Value.EnumerateArray())
                    {
                        array.Add(RoleAssignment.DeserializeRoleAssignment(item));
                    }
                    roles = array;
                    continue;
                }
            }
            return new User(roles, id.Value, type);
        }
    }
}
