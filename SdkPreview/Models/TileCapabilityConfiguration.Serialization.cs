// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License.

// <auto-generated/>

#nullable disable

using System.Collections.Generic;
using System.Text.Json;
using Azure.Core;

namespace Microsoft.Azure.IoTCentral.Preview.Models
{
    public partial class TileCapabilityConfiguration : IUtf8JsonSerializable
    {
        void IUtf8JsonSerializable.Write(Utf8JsonWriter writer)
        {
            writer.WriteStartObject();
            if (Optional.IsCollectionDefined(Capabilities))
            {
                writer.WritePropertyName("capabilities");
                writer.WriteStartArray();
                foreach (var item in Capabilities)
                {
                    writer.WriteObjectValue(item);
                }
                writer.WriteEndArray();
            }
            writer.WriteEndObject();
        }

        internal static TileCapabilityConfiguration DeserializeTileCapabilityConfiguration(JsonElement element)
        {
            Optional<IList<TileCapability>> capabilities = default;
            foreach (var property in element.EnumerateObject())
            {
                if (property.NameEquals("capabilities"))
                {
                    if (property.Value.ValueKind == JsonValueKind.Null)
                    {
                        property.ThrowNonNullablePropertyIsNull();
                        continue;
                    }
                    List<TileCapability> array = new List<TileCapability>();
                    foreach (var item in property.Value.EnumerateArray())
                    {
                        array.Add(TileCapability.DeserializeTileCapability(item));
                    }
                    capabilities = array;
                    continue;
                }
            }
            return new TileCapabilityConfiguration(Optional.ToList(capabilities));
        }
    }
}
